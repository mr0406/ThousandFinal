@foreach(var player in Players)
{
    <p>@player.Name, @player.Points points, @player.PointsInCurrentRound roundPoints, @player.PointsToAchieve toAchieve</p>
}

@code {
    [Parameter] public List<UserModel> Players { get; set; }
}
